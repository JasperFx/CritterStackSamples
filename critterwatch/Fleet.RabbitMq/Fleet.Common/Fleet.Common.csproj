<Project Sdk="Microsoft.NET.Sdk">

  <!-- Tiny shared helper library for the Fleet.RabbitMq sample. Holds the one piece of glue every
       project needs: resolving the RabbitMQ broker URI and Postgres connection string from either the
       Aspire-injected ConnectionStrings__* environment or a localhost docker-compose fallback (so each
       project still F5-runs standalone). Kept transport/store-agnostic and dependency-light on purpose. -->

</Project>
